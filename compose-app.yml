services:
  drift-base:
    build:
      context: .
      tags:
        - app-drift-base:latest
    ports:
      - "8080:8080"
    env_file:
      - scripts/local.env
    networks:
      - app
      - backend

networks:
  app:
    name: app
  backend:
    external: true
    name: backend
