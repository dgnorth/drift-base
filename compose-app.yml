version: '3.5'
services:
  drift-base:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - scripts/local.env
    networks:
      - app
      - backend

networks:
  app:
    name: app
  backend:
    name: backend
